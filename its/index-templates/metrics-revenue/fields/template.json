{
  "index_patterns": ["metric_revernue_*"],
  "priority": 1,
  "template": {
    "settings": {
      "index": {
        "codec" : "best_compression",
        "mapping": {
          "total_fields": {
            "limit": 2000
          }
        },
        "default_pipeline": "metrics_revenue_mapping"
      }
    }
  }
}