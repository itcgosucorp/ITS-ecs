---
name: no-ecs-example
fields:
  # ECS basic fields
  base:
    fields:
      "@timestamp": {}
  user:
    fields:
      id: {}
  event:
    fields:
      original: {}
  its_task_log:
    fields: "*"
  
