{
  "index_patterns": ["metrics_*_*_login"],
  "priority": 1,
  "template": {
    "settings": {
      "index": {
        "codec" : "best_compression",
        "mapping": {
          "total_fields": {
            "limit": 2000
          }
        },
        "default_pipeline": "metrics_login_mapping"
      }
    }
  }
}