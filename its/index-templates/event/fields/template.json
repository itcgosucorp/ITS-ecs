{
  "index_patterns": ["event_*_*", "test_event_*_*"],
  "priority": 1,
  "template": {
    "settings": {
      "index": {
        "codec" : "best_compression",
        "mapping": {
          "total_fields": {
            "limit": 2000
          }
        },
        "number_of_replicas": 0,
        "auto_expand_replicas": "false"
      }
    }
  }
}