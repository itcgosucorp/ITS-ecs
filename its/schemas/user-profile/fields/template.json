{
  "index_patterns": ["user_profile_*"],
  "priority": 1,
  "template": {
    "settings": {
      "index": {
        "codec" : "best_compression",
        "mapping": {
          "total_fields": {
            "limit": 2000
          }
        },
        "default_pipeline": "user_profile"
      }
    }
  }
}